# Deploy button / wrangler 会将这些键作为 Worker Secret 提示配置。
# ADMIN_PASSWORD：QuickShare 管理后台登录密码。
ADMIN_PASSWORD=replace-me
# COOKIE_SIGNING_KEY：至少 32 字节的随机字符串，用于签名会话 Cookie。
COOKIE_SIGNING_KEY=replace-with-a-random-32-byte-secret
